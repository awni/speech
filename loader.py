# Loader is an interface and exposes a few functions:
# train_batches, val_batches, input_dim, output_dim..
# Any subclass of the loader must at the very least implement a function which returns a list of files and corresponding transcriptions.
